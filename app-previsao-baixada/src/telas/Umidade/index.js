// Umidade.js
import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Umidade = () => {
  return (
    <View style={styles.container}>
      <Text>Umidade Screen</Text>
      {}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default Umidade;
